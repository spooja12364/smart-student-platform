[pytest]
markers =
    e2e: End-to-end tests run against a live or local browser
    ui_ux: UI/UX design, visual, layout, and responsiveness tests
    functional: Core functional, database interaction, navigation, and user flow tests
    unit: Backend model serialization, helper functions, and logic unit tests
    validation: Form validation, size limits, input restriction, and security validation tests
